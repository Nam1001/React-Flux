---
trigger: always_on
---

Public API must be minimal — if in doubt, leave it out
Never change or remove a public exported API without a major version bump
All options must be optional with sensible defaults
Error messages must be clear and actionable — tell the developer how to fix it