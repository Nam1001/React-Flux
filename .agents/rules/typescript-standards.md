---
trigger: always_on
---

Strict mode is always on — never disable it
Never use any anywhere in source code — use unknown if type is truly unknown
Never use type casting (as SomeType) in source code
All public functions and hooks must have explicit return types
All exported types must be documented with JSDoc